[project]
name = "gwrefpy"
version = "0.4.1"
description = "A Python package for detection of deviations in groundwater time series."
readme = "README.md"
license = "MIT"
authors = [
    { name = "Anders Retzner", email = "anders.retzner@gmail.com" },
    { name = "Erik Toller" },
    { name = "Tobias Erlström" },
]
requires-python = ">=3.11"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "matplotlib>=3.6.3",
    "numpy>=1.24.4",
    "pandas>=2.2.1",   # 2.2.0 broken: string-offset comparison in timeseries.py
    "scipy>=1.10.1",
]

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "ruff>=0.12.10",
    "sphinx",
    "pydata-sphinx-theme",
    "sphinx-copybutton",
    "sphinx-autobuild",
    "myst-nb>=1.3.0",
    "sphinx-design>=0.6.1",
    "sgu-client"
]
recommended = ["notebook", "sgu-client"]

[tool.ruff]
line-length = 88
target-version = "py311"
exclude = ["*.ipynb"]

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
